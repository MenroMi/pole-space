# Layout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `(main)` and `(auth)` layouts with a glassmorphic Header, Footer, and reusable `PageShell` grid component.

**Architecture:** `(main)/layout.tsx` renders Header + Footer around `{children}`. Pages that need an aside wrap their content in `<PageShell aside={...}>`. `(auth)/layout.tsx` is an independent centered layout with the wordmark and no nav.

**Tech Stack:** Next.js 16 App Router, Tailwind CSS v4, `next/font/google` (Space Grotesk + Manrope), inline SVG icons, Vitest + RTL.

---

## File Map

| File                                       | Action | Responsibility                                               |
| ------------------------------------------ | ------ | ------------------------------------------------------------ |
| `src/app/layout.tsx`                       | Modify | Swap fonts: Geist → Space Grotesk + Manrope                  |
| `src/app/globals.css`                      | Modify | Design tokens + dark palette as default                      |
| `src/shared/components/PageShell.tsx`      | Create | CSS Grid wrapper — aside + main                              |
| `src/shared/components/HeaderNav.tsx`      | Create | Client: active nav links via usePathname                     |
| `src/shared/components/Header.tsx`         | Create | RSC: glassmorphic sticky header + session-aware account link |
| `src/shared/components/Footer.tsx`         | Create | Client: bottom nav with active state                         |
| `src/app/(main)/layout.tsx`                | Create | Composes Header + {children} + Footer                        |
| `src/app/(auth)/layout.tsx`                | Create | Centered layout with wordmark                                |
| `src/shared/components/PageShell.test.tsx` | Create | PageShell unit tests                                         |
| `src/shared/components/HeaderNav.test.tsx` | Create | HeaderNav unit tests                                         |
| `src/shared/components/Header.test.tsx`    | Create | Header unit tests                                            |
| `src/shared/components/Footer.test.tsx`    | Create | Footer unit tests                                            |

---

## Task 1: Fonts + Design Tokens

**Files:**

- Modify: `src/app/layout.tsx`
- Modify: `src/app/globals.css`

> Before writing code, read `node_modules/next/dist/docs/` for any Next.js 16-specific font or layout API changes.

- [ ] **Step 1: Update `src/app/layout.tsx`**

```tsx
import type { Metadata } from 'next'
import { Space_Grotesk, Manrope } from 'next/font/google'
import './globals.css'

const spaceGrotesk = Space_Grotesk({
  variable: '--font-space-grotesk',
  subsets: ['latin'],
})

const manrope = Manrope({
  variable: '--font-manrope',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'Kinetic Gallery — Pole Artistry Platform',
  description: 'Catalog of pole dance moves',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${spaceGrotesk.variable} ${manrope.variable} antialiased`}>{children}</body>
    </html>
  )
}
```

- [ ] **Step 2: Replace `src/app/globals.css`**

```css
@import 'tailwindcss';

:root {
  --surface: #131313;
  --surface-container-lowest: #0e0e0e;
  --surface-container-low: #1b1b1b;
  --surface-container: #1f1f1f;
  --surface-container-high: #2a2a2a;
  --surface-container-highest: #353535;
  --on-surface: #e2e2e2;
  --on-surface-variant: #cdc3d2;
  --primary: #dcb8ff;
  --primary-container: #8458b3;
  --secondary-container: #52416c;
  --on-secondary-container: #c5afe2;
  --outline: #978e9b;
  --outline-variant: #4b4450;
}

@theme inline {
  --color-surface: var(--surface);
  --color-surface-lowest: var(--surface-container-lowest);
  --color-surface-low: var(--surface-container-low);
  --color-surface-container: var(--surface-container);
  --color-surface-high: var(--surface-container-high);
  --color-surface-highest: var(--surface-container-highest);
  --color-on-surface: var(--on-surface);
  --color-on-surface-variant: var(--on-surface-variant);
  --color-primary: var(--primary);
  --color-primary-container: var(--primary-container);
  --color-secondary-container: var(--secondary-container);
  --color-on-secondary-container: var(--on-secondary-container);
  --color-outline: var(--outline);
  --color-outline-variant: var(--outline-variant);
  --font-sans: var(--font-manrope);
  --font-display: var(--font-space-grotesk);
}

body {
  background-color: var(--surface);
  color: var(--on-surface);
  font-family: var(--font-manrope), Arial, sans-serif;
}
```

- [ ] **Step 3: Verify build compiles**

```bash
npm run build
```

Expected: build succeeds, no font or CSS errors.

- [ ] **Step 4: Commit**

```bash
git add src/app/layout.tsx src/app/globals.css
git commit -m "chore: swap fonts to Space Grotesk + Manrope, add design tokens"
```

---

## Task 2: PageShell Component

**Files:**

- Create: `src/shared/components/PageShell.tsx`
- Create: `src/shared/components/PageShell.test.tsx`

- [ ] **Step 1: Write failing test**

Create `src/shared/components/PageShell.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import PageShell from './PageShell'

describe('PageShell', () => {
  it('renders aside content', () => {
    render(
      <PageShell aside={<div>filters</div>}>
        <div>main</div>
      </PageShell>,
    )
    expect(screen.getByText('filters')).toBeInTheDocument()
  })

  it('renders children in main area', () => {
    render(
      <PageShell aside={<div>aside</div>}>
        <div>main content</div>
      </PageShell>,
    )
    expect(screen.getByText('main content')).toBeInTheDocument()
  })

  it('places aside inside an <aside> element', () => {
    render(
      <PageShell aside={<div data-testid="aside-content">filters</div>}>
        <div>main</div>
      </PageShell>,
    )
    const asideContent = screen.getByTestId('aside-content')
    expect(asideContent.closest('aside')).toBeInTheDocument()
  })

  it('places children inside a <main> element', () => {
    render(
      <PageShell aside={<div>aside</div>}>
        <div data-testid="main-content">content</div>
      </PageShell>,
    )
    const mainContent = screen.getByTestId('main-content')
    expect(mainContent.closest('main')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- --run src/shared/components/PageShell.test.tsx
```

Expected: FAIL — `Cannot find module './PageShell'`

- [ ] **Step 3: Create `src/shared/components/PageShell.tsx`**

```tsx
interface PageShellProps {
  aside: React.ReactNode
  children: React.ReactNode
}

export default function PageShell({ aside, children }: PageShellProps) {
  return (
    <div className="grid flex-1 grid-cols-[260px_1fr]">
      <aside className="bg-surface-low">{aside}</aside>
      <main className="bg-surface">{children}</main>
    </div>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
npm run test -- --run src/shared/components/PageShell.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/PageShell.tsx src/shared/components/PageShell.test.tsx
git commit -m "feat: add PageShell grid component"
```

---

## Task 3: HeaderNav (Client Component)

**Files:**

- Create: `src/shared/components/HeaderNav.tsx`
- Create: `src/shared/components/HeaderNav.test.tsx`

- [ ] **Step 1: Write failing test**

Create `src/shared/components/HeaderNav.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { usePathname } from 'next/navigation'
import HeaderNav from './HeaderNav'

vi.mock('next/navigation', () => ({ usePathname: vi.fn() }))

beforeEach(() => vi.clearAllMocks())

describe('HeaderNav', () => {
  it('renders Catalog and Moves links with correct hrefs', () => {
    vi.mocked(usePathname).mockReturnValue('/')
    render(<HeaderNav />)
    expect(screen.getByRole('link', { name: 'Catalog' })).toHaveAttribute('href', '/catalog')
    expect(screen.getByRole('link', { name: 'Moves' })).toHaveAttribute('href', '/moves')
  })

  it('applies active class to the link matching the current path', () => {
    vi.mocked(usePathname).mockReturnValue('/catalog')
    render(<HeaderNav />)
    expect(screen.getByRole('link', { name: 'Catalog' }).className).toContain('text-primary')
  })

  it('does not apply active class to non-matching links', () => {
    vi.mocked(usePathname).mockReturnValue('/catalog')
    render(<HeaderNav />)
    expect(screen.getByRole('link', { name: 'Moves' }).className).not.toContain('text-primary')
  })

  it('marks /moves as active when pathname starts with /moves', () => {
    vi.mocked(usePathname).mockReturnValue('/moves/abc123')
    render(<HeaderNav />)
    expect(screen.getByRole('link', { name: 'Moves' }).className).toContain('text-primary')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- --run src/shared/components/HeaderNav.test.tsx
```

Expected: FAIL — `Cannot find module './HeaderNav'`

- [ ] **Step 3: Create `src/shared/components/HeaderNav.tsx`**

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'

const NAV_LINKS = [
  { label: 'Catalog', href: '/catalog' },
  { label: 'Moves', href: '/moves' },
]

export default function HeaderNav() {
  const pathname = usePathname()

  return (
    <nav className="flex gap-8">
      {NAV_LINKS.map(({ label, href }) => (
        <Link
          key={href}
          href={href}
          className={`text-sm font-medium transition-colors ${
            pathname.startsWith(href)
              ? 'text-primary'
              : 'text-on-surface-variant hover:text-on-surface'
          }`}
        >
          {label}
        </Link>
      ))}
    </nav>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
npm run test -- --run src/shared/components/HeaderNav.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/HeaderNav.tsx src/shared/components/HeaderNav.test.tsx
git commit -m "feat: add HeaderNav client component with active link state"
```

---

## Task 4: Header (Server Component)

**Files:**

- Create: `src/shared/components/Header.tsx`
- Create: `src/shared/components/Header.test.tsx`

- [ ] **Step 1: Write failing test**

Create `src/shared/components/Header.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { auth } from '@/shared/lib/auth'
import Header from './Header'

vi.mock('@/shared/lib/auth', () => ({ auth: vi.fn() }))
vi.mock('./HeaderNav', () => ({ default: () => <nav data-testid="header-nav" /> }))

const mockAuth = auth as ReturnType<typeof vi.fn>

beforeEach(() => vi.clearAllMocks())

describe('Header', () => {
  it('renders wordmark linking to /', async () => {
    mockAuth.mockResolvedValue(null)
    render(await Header())
    expect(screen.getByRole('link', { name: /kinetic gallery/i })).toHaveAttribute('href', '/')
  })

  it('renders HeaderNav', async () => {
    mockAuth.mockResolvedValue(null)
    render(await Header())
    expect(screen.getByTestId('header-nav')).toBeInTheDocument()
  })

  it('links account icon to /profile when session exists', async () => {
    mockAuth.mockResolvedValue({ user: { id: '1', role: 'USER' } })
    render(await Header())
    expect(screen.getByTestId('account-link')).toHaveAttribute('href', '/profile')
  })

  it('links account icon to /login when no session', async () => {
    mockAuth.mockResolvedValue(null)
    render(await Header())
    expect(screen.getByTestId('account-link')).toHaveAttribute('href', '/login')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- --run src/shared/components/Header.test.tsx
```

Expected: FAIL — `Cannot find module './Header'`

- [ ] **Step 3: Create `src/shared/components/Header.tsx`**

```tsx
import Link from 'next/link'
import { auth } from '@/shared/lib/auth'
import HeaderNav from './HeaderNav'

export default async function Header() {
  const session = await auth()
  const accountHref = session ? '/profile' : '/login'

  return (
    <header
      className="sticky top-0 z-50 flex h-16 items-center justify-between px-8 backdrop-blur-xl"
      style={{ backgroundColor: 'rgba(19, 19, 19, 0.8)' }}
    >
      <Link
        href="/"
        className="font-display text-lg font-semibold tracking-tight text-on-surface lowercase"
      >
        kinetic gallery
      </Link>

      <HeaderNav />

      <div className="flex items-center gap-4">
        <button
          type="button"
          aria-label="Search"
          className="text-on-surface-variant transition-colors hover:text-on-surface"
        >
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            aria-hidden="true"
          >
            <circle cx="11" cy="11" r="8" />
            <path d="m21 21-4.35-4.35" />
          </svg>
        </button>

        <Link
          href={accountHref}
          data-testid="account-link"
          aria-label="Account"
          className="text-on-surface-variant transition-colors hover:text-on-surface"
        >
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            strokeWidth="2"
            aria-hidden="true"
          >
            <circle cx="12" cy="8" r="4" />
            <path d="M4 20c0-4 3.6-7 8-7s8 3 8 7" />
          </svg>
        </Link>
      </div>
    </header>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
npm run test -- --run src/shared/components/Header.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/Header.tsx src/shared/components/Header.test.tsx
git commit -m "feat: add Header RSC with glassmorphism and session-aware account link"
```

---

## Task 5: Footer (Client Component)

**Files:**

- Create: `src/shared/components/Footer.tsx`
- Create: `src/shared/components/Footer.test.tsx`

- [ ] **Step 1: Write failing test**

Create `src/shared/components/Footer.test.tsx`:

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { usePathname } from 'next/navigation'
import Footer from './Footer'

vi.mock('next/navigation', () => ({ usePathname: vi.fn() }))

beforeEach(() => vi.clearAllMocks())

describe('Footer', () => {
  it('renders all nav links with correct hrefs', () => {
    vi.mocked(usePathname).mockReturnValue('/')
    render(<Footer />)
    expect(screen.getByRole('link', { name: /gallery/i })).toHaveAttribute('href', '/catalog')
    expect(screen.getByRole('link', { name: /moves/i })).toHaveAttribute('href', '/moves')
    expect(screen.getByRole('link', { name: /profile/i })).toHaveAttribute('href', '/profile')
  })

  it('applies active class to Gallery when pathname is /catalog', () => {
    vi.mocked(usePathname).mockReturnValue('/catalog')
    render(<Footer />)
    expect(screen.getByRole('link', { name: /gallery/i }).className).toContain('text-primary')
  })

  it('does not apply active class to non-matching links', () => {
    vi.mocked(usePathname).mockReturnValue('/catalog')
    render(<Footer />)
    expect(screen.getByRole('link', { name: /profile/i }).className).not.toContain('text-primary')
  })

  it('renders the version label', () => {
    vi.mocked(usePathname).mockReturnValue('/')
    render(<Footer />)
    expect(screen.getByText(/kinetic gallery v\.0\.1/i)).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm run test -- --run src/shared/components/Footer.test.tsx
```

Expected: FAIL — `Cannot find module './Footer'`

- [ ] **Step 3: Create `src/shared/components/Footer.tsx`**

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'

const NAV_ITEMS = [
  {
    label: 'Gallery',
    href: '/catalog',
    icon: (
      <svg
        width="18"
        height="18"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        aria-hidden="true"
      >
        <rect x="3" y="3" width="7" height="7" />
        <rect x="14" y="3" width="7" height="7" />
        <rect x="3" y="14" width="7" height="7" />
        <rect x="14" y="14" width="7" height="7" />
      </svg>
    ),
  },
  {
    label: 'Moves',
    href: '/moves',
    icon: (
      <svg
        width="18"
        height="18"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        aria-hidden="true"
      >
        <polygon points="5 3 19 12 5 21 5 3" />
      </svg>
    ),
  },
  {
    label: 'Profile',
    href: '/profile',
    icon: (
      <svg
        width="18"
        height="18"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        strokeWidth="2"
        aria-hidden="true"
      >
        <circle cx="12" cy="8" r="4" />
        <path d="M4 20c0-4 3.6-7 8-7s8 3 8 7" />
      </svg>
    ),
  },
]

export default function Footer() {
  const pathname = usePathname()

  return (
    <footer className="flex h-14 items-center justify-between bg-surface-lowest px-8">
      <nav className="flex gap-6">
        {NAV_ITEMS.map(({ label, href, icon }) => (
          <Link
            key={href}
            href={href}
            className={`flex items-center gap-2 text-sm transition-colors ${
              pathname.startsWith(href)
                ? 'text-primary'
                : 'text-on-surface-variant hover:text-on-surface'
            }`}
          >
            {icon}
            <span>{label}</span>
          </Link>
        ))}
      </nav>
      <span className="font-sans text-xs tracking-widest text-on-surface-variant uppercase">
        kinetic gallery v.0.1
      </span>
    </footer>
  )
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
npm run test -- --run src/shared/components/Footer.test.tsx
```

Expected: 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/Footer.tsx src/shared/components/Footer.test.tsx
git commit -m "feat: add Footer client component with active nav state"
```

---

## Task 6: Main Layout

**Files:**

- Create: `src/app/(main)/layout.tsx`

No unit tests for the layout itself — it's a thin composition of already-tested components. The build verifies it works.

- [ ] **Step 1: Create `src/app/(main)/layout.tsx`**

```tsx
import Header from '@/shared/components/Header'
import Footer from '@/shared/components/Footer'

export default function MainLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen flex-col bg-surface">
      <Header />
      <div className="flex flex-1 flex-col">{children}</div>
      <Footer />
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds. All `(main)` routes (/, /catalog, /profile, /moves/[id]) now render with Header and Footer.

- [ ] **Step 3: Run all tests**

```bash
npm run test -- --run
```

Expected: all tests PASS.

- [ ] **Step 4: Commit**

```bash
git add src/app/(main)/layout.tsx
git commit -m "feat: add (main) layout with Header and Footer"
```

---

## Task 7: Auth Layout

**Files:**

- Create: `src/app/(auth)/layout.tsx`

- [ ] **Step 1: Create `src/app/(auth)/layout.tsx`**

```tsx
import Link from 'next/link'

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-10 bg-surface px-4 py-16">
      <Link
        href="/"
        className="font-display text-2xl font-semibold tracking-tight text-on-surface lowercase"
      >
        kinetic gallery
      </Link>
      {children}
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
npm run build
```

Expected: build succeeds. Auth routes (/login, /signup, /verify-email) now render with the wordmark above the form.

- [ ] **Step 3: Run all tests**

```bash
npm run test -- --run
```

Expected: all tests PASS.

- [ ] **Step 4: Commit**

```bash
git add src/app/(auth)/layout.tsx
git commit -m "feat: add (auth) layout with centered wordmark"
```

---

## Done

All 13 test files should pass. Run the full suite one final time:

```bash
npm run test -- --run
```

Then verify the build:

```bash
npm run build
```
